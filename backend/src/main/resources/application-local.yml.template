# ============================================================
# application-local.yml — LOCAL OVERRIDES (gitignored)
# Copy this template to application-local.yml and fill in secrets.
# NEVER commit this file or the filled version to git.
# ============================================================

# DeepSeek API Key (REQUIRED — get from https://platform.deepseek.com)
DEEPSEEK_API_KEY: sk-your-deepseek-api-key-here

# Optional: override model
# DEEPSEEK_MODEL: deepseek-chat

# Optional: override DB credentials
# DB_PASSWORD: your_local_db_password
# DB_READONLY_PASSWORD: your_local_readonly_password
